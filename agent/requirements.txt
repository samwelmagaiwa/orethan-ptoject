# Orethan Biometric Agent — Python dependencies
# Install with:  pip install -r requirements.txt

# WebSocket server (core requirement)
websockets>=12.0

# USB device enumeration (for auto-detection and hot-plug)
# On Windows: also install Zadig USB driver if your scanner isn't detected
#   → https://zadig.akeo.ie/
pyusb>=1.2.1

# Optional: libusb backend for pyusb (Windows users — if pyusb can't find backend)
# pip install libusb-package    (bundles the DLL automatically)
libusb-package>=1.0.26.1
